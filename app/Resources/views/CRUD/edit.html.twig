{% extends  'SonataAdminBundle:CRUD:edit.html.twig' %}
{%  block javascripts %}
      {{ parent() }}
      {{ tinymce_init() }}
      <script>
       $(function(){
	    $(".IamTagable").select2({ tags: [$(this).val()] });
       });
      </script>
{% endblock %}
